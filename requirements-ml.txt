# Optional ML stack (TensorFlow disease model). Install may fail on Python 3.13+ or low RAM.
# start.bat / start.sh attempt this after core requirements.txt
tensorflow>=2.15.0,<2.20.0
opencv-python-headless>=4.9.0
